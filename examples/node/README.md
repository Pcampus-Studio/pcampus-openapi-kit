# Node Examples

Implementation examples for consuming `pcampus-openapi-kit` specs in Node.js services.

Planned content:

- Express or Fastify handlers generated from operations
- shared error middleware aligned with `ErrorResponse`
- OpenAPI-driven integration tests
