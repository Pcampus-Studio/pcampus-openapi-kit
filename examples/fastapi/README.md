# FastAPI Examples

Implementation examples for consuming `pcampus-openapi-kit` specs in FastAPI applications.

Planned content:

- Pydantic models generated from shared schemas
- dependency injection for auth and tenant scoping
- response validation against OpenAPI components
