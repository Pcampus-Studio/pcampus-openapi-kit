# Laravel Examples

Implementation examples for consuming `pcampus-openapi-kit` specs in Laravel applications.

Planned content:

- route and controller generation from OpenAPI
- Form Request validation mapped to shared error schemas
- contract tests against approved specs
