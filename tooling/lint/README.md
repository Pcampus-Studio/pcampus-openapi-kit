# Lint Tooling

OpenAPI lint rules and configuration for `pcampus-openapi-kit`.

Planned content:

- Spectral rulesets aligned with repository conventions
- CI checks for descriptions, error responses, and agent hints
